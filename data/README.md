# data/

Generated output only. Everything here must be reproducible from `source/*.xlsx`
via `tools/build_data.py`. Do not hand-edit files in this directory — fix the
workbook or the build script instead.

Planned outputs: `boxes.json`, `seais.json`, `population.json`, `regions.json`,
and later `links.json`, `box-segments.json`.
